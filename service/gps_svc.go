package service

func AddGps(dto GpsDto, out PostGpsRspData) {

	//gps:=model.Gps{}
	/*	if dto.Md5 != "" {
		err := gps.QueryByMd5(args.Md5)
		if err != nil && err != gorm.RecordNotFound {
			util.Logger.Error("[QueryByMd5][err:%v]", err)
			return err
		} else if err != gorm.RecordNotFound {
			reply.SetCode(code.GSP_EXISTS)
			err = nil
			return
		}
	}*/
}
